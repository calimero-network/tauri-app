{
  "identifier": "migrated",
  "description": "IPC access for the desktop's own window: the app commands plus the few plugin APIs its frontend calls directly.",
  "local": true,
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-unminimize",
    "core:window:allow-show",
    "core:window:allow-set-focus",
    "process:allow-restart",
    "allow-app-commands"
  ]
}
