<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- WebRTC video calls (Mero Meet): allow the hardened-runtime app to use
         the camera and microphone. Paired with the NSCamera/NSMicrophone usage
         strings in Info.plist and the WKWebView media-permission handler. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Universal Links: associate the app with links.calimero.network so the OS
         routes https://links.calimero.network/<slug>/<action>?<params> into
         on_open_url.
         IMPORTANT: this is a RESTRICTED entitlement (com.apple.developer.*). A
         Developer-ID-signed release that carries it MUST embed a matching
         provisioning profile at Contents/embedded.provisionprofile (App ID
         BTC39PAGX2.network.calimero.desktop, Associated Domains capability), or
         AMFI SIGKILLs the app at launch ("can't be opened" / launchd err 153).
         CI supplies the profile via APPLE_PROVISIONING_PROFILE + the
         bundle.macOS.files override in build-macos.yml. Local/ad-hoc builds do
         not associate and don't hit this — the calimero:// custom scheme path is
         unaffected either way. -->
    <key>com.apple.developer.associated-domains</key>
    <array>
        <string>applinks:links.calimero.network</string>
    </array>
</dict>
</plist>
