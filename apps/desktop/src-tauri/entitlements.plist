<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- WebRTC video calls (Mero Meet): allow the hardened-runtime app to use
         the camera and microphone. Paired with the NSCamera/NSMicrophone usage
         strings in Info.plist and the WKWebView media-permission handler. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Universal Links: associate the app with links.calimero.network so the OS
         routes https://links.calimero.network/<slug>/<action>?<params> into
         on_open_url. Only takes effect in a Developer-ID-signed release build
         whose Team ID matches the AASA (BTC39PAGX2.network.calimero.desktop);
         the local ad-hoc build won't associate — the calimero:// custom scheme
         path is unaffected and remains testable unsigned. -->
    <key>com.apple.developer.associated-domains</key>
    <array>
        <string>applinks:links.calimero.network</string>
    </array>
</dict>
</plist>
