<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- WebRTC video calls (Mero Meet): allow the hardened-runtime app to use
         the camera and microphone. Paired with the NSCamera/NSMicrophone usage
         strings in Info.plist and the WKWebView media-permission handler. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- NOTE: do NOT add com.apple.developer.associated-domains (universal links)
         here. On a hardened-runtime Developer-ID release it is a RESTRICTED
         entitlement that AMFI refuses to authorize — the app is SIGKILLed at
         launch ("can't be opened" / launchd err 153) on every machine. Embedding
         a Developer-ID provisioning profile does NOT rescue it (verified on the
         notarized v0.0.76 DMG). Universal-links-on-Developer-ID is unsolved;
         deep-link invitations use the calimero:// custom scheme, which needs no
         entitlement. See git history of #165 / v0.0.76 before retrying. -->
</dict>
</plist>
