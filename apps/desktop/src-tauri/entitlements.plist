<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- WebRTC video calls (Mero Meet): allow the hardened-runtime app to use
         the camera and microphone. Paired with the NSCamera/NSMicrophone usage
         strings in Info.plist and the WKWebView media-permission handler. -->
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Universal Links (https://links.calimero.network/...). These three are
         MANAGED (com.apple.developer.*/application-identifier) restricted
         entitlements. AMFI only authorizes them at launch if the app embeds a
         Developer-ID provisioning profile at Contents/embedded.provisionprofile
         whose DeveloperCertificates include the SAME cert that signs the app —
         otherwise it's SIGKILLed at spawn ("can't be opened" / launchd err 153).
         CI supplies that profile from APPLE_PROVISIONING_PROFILE (build-macos.yml)
         and signs with the matching Developer ID cert. All three keys must be
         present or the profile match fails. The account has two Developer ID
         Application certs — the profile MUST embed the one CI signs with (C=ES,
         serial 3F784069…). Verified end-to-end on v0.0.76. Do NOT add these to
         the nested calimero-shell (see shell.entitlements.plist). -->
    <key>com.apple.developer.associated-domains</key>
    <array>
        <string>applinks:links.calimero.network</string>
    </array>
    <key>com.apple.application-identifier</key>
    <string>BTC39PAGX2.network.calimero.desktop</string>
    <key>com.apple.developer.team-identifier</key>
    <string>BTC39PAGX2</string>
</dict>
</plist>
