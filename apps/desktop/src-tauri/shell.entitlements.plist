<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Entitlements for the nested calimero-shell webview host. Intentionally a
         copy of entitlements.plist MINUS com.apple.developer.associated-domains:
         only the main app handles universal links, and giving the restricted
         entitlement to a nested helper (which the app's embedded provisioning
         profile doesn't cleanly authorize under its own signing identifier) can
         get the helper SIGKILLed by AMFI. The shell just needs the hardened-
         runtime + media + network entitlements to host WKWebViews (Mero Meet). -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
