<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Entitlements for the nested calimero-shell webview host. Deliberately the
         app entitlements MINUS the managed universal-links keys
         (associated-domains / application-identifier / team-identifier): only the
         main app handles universal links, and those managed entitlements on a
         nested helper have no matching profile of their own. The shell only needs
         hardened-runtime + media + network to host WKWebViews (Mero Meet).
         Verified: with the main app carrying the profile + managed entitlements
         and the shell signed with THIS file (no managed keys), the app launches. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.network.server</key>
    <true/>
    <key>com.apple.security.network.client</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
